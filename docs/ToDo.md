```bash
install
	|--- bin_backend
	|--- etc
			|--- fs
			|--- net
			|--- process
			|--- memory
	|--- brige
	|		|--- prometheus_client
	|---visualization
			|--- run docker(grafana + prometheus)
	|--- run.sh/py
```

run.sh: 启动run docker, prometheus_client。prometheus_client启动backend后端的工具 
