内存泄露问题在线排查思路及步骤

- 通过 `top` 找到内存泄露的进程
- 通过 `pmap` 找到内存泄露的地址及范围
- 通过 `gcore` 对进程内存进行快照
- 通过 `gdb` 加载内存信息
- 通过 `dump binary` 导出泄露内存的内容
- 通过 `vim` 查看内存内容
- 根据内存中的内容，锁定对应的代码段，进行排查修复

请优先按照上述思路进行排查，不允许私自调用Valgrind，如果确实需要调用Valgrind，请征得同意后进行。

